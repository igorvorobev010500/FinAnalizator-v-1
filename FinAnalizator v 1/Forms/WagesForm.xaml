<Window x:Class="FinAnalizator_v_1.Forms.WagesForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        xmlns:local="clr-namespace:FinAnalizator_v_1.Forms"
        mc:Ignorable="d"
        Icon="/src/Assets/Icons/Icon.ico"
        Title="Заработная плата" Height="300" Width="400">
    <Window.Resources>
        <Style x:Key="MessageButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <TextBlock x:Name="WagesTextBlock" 
                   Text="Введите ваш доход в неделю" 
                   TextWrapping="Wrap" 
                   FontSize="16" 
                   Margin="20,30,20,80" 
                   VerticalAlignment="Top" 
                   HorizontalAlignment="Center" 
                   TextAlignment="Center"/>
        <TextBox x:Name="WagesText"
                 Width="200"
                 Height="30"
                 VerticalAlignment="Center"
                 HorizontalAlignment="Center"
                 Margin="0,0,0,50"
                 FontSize="16"
                 TextAlignment="Center"/>
        
        <Button Content="Подтвердить" 
                Padding="10,5"
                VerticalAlignment="Bottom"
                Margin="50" Width = "120"
                Click="CloseForm_Click"
                Style="{StaticResource MessageButtonStyle}" />
    </Grid>
</Window>
